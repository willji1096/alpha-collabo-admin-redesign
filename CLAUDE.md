# 알파 콜라보 어드민 개선 프로젝트

## 프로젝트 개요
- **목적**: 기존 알파 콜라보 어드민의 화면 디자인 개선
- **우리 역할**: 디자인(HTML/CSS 목업) 산출물 제작 — 개발은 개발팀이 직접 수행
- **산출물**: 개선된 화면 디자인 (HTML/CSS, 필요 시 Figma)
- **전달 방식**: 코드가 Source of Truth — `docs/DESIGN.md` + `css/common.css` + `css/admin.css` + 개별 화면 HTML

## 서비스 플로우 (정정판)
> 과거 "클라이언트가 어드민에서 캠페인 등록"은 **부정확**. 캠페인 생성 주체는 매니저다.

1. **브랜드사 ↔ 매니저 상담** → **매니저가 캠페인 생성** (브랜드사 직접 생성 아님)
2. 캠페인이 **알파콜라보 앱(인플루언서용)** 에 등록·노출
3. **인플루언서가 앱에서 지원** (예: 10명 모집에 100명 지원)
4. 지원자가 **브랜드사 어드민 → 신청서 관리(신청목록)** 에 집계
5. **브랜드사가 어드민에서 리뷰어 선정 → 검수 → 수정 → 등록완료**
6. 병행: 브랜드사가 인플루언서 탐색/방문예정에서 **능동 제안**

## 핵심 정책 (도메인 — 반드시 준수)

### Surface 4개 구분 (혼동 금지)
- **최고관리자 어드민** = 알파콜라보 운영/매니저용. 캠페인 생성·전체 총괄. **우리 작업 아님.**
- **브랜드사 어드민(웹) + `app/phone`** = 브랜드사(광고주)용. 신청서 관리·탐색·선정. **우리가 작업 중.**
- **알파콜라보 앱(인플루언서용)** = 캠페인 보고 지원. 별개(`~/Desktop/_cursor/alphacollabo*`). 우리 작업 아님.
- 경로 주의: `_cursor/alphacollabo`, `alphacollabo-new`는 인플루언서 앱이지 어드민 아님 — 자주 혼동.
- **실사용자 = 클라이언트(광고주 마케팅 담당/사장)가 직접 사용** (매니저 대행 아님). 화면 눈높이는 "광고주 본인이 직접 보고 채택·검수 결정". 페르소나 상세: `docs/CONCEPT-페르소나.md`.

### 브랜드사 어드민의 역할 경계
- 브랜드사 어드민은 "캠페인 만드는 곳"이 아니라 **"이미 만들어진 캠페인을 운영하는 곳"**. 풀스펙 캠페인 생성 UI는 매니저 영역 — 임의 확대 금지.
- **"신청목록" = 우리 캠페인에 지원한 인플루언서들** → 카드 주체는 캠페인이 아니라 인플루언서(아바타/핸들 강조).

### 인플루언서 직접소통 금지 (핵심 해자)
- 인플루언서는 알파콜라보의 자산. 클라이언트↔인플루언서 **직접 소통 창구 금지**(실시간 채팅·쪽지 모두) — 우회·직접계약(디스인터미디에이션) 방지. 소통은 매니저 경유(매니저↔클라이언트는 카카오).
- 구현 결정(완료): `influencer-chat/manager-chat/messages.html` 삭제, 하단 탭은 메시지 탭 제거 후 **인플루언서 탭으로 교체(5탭: 홈/캠페인/인플루언서/일정/더보기)**. 알림센터는 유지하되 직접소통 버튼 제거, 액션 CTA(충전/검수/제안)만.

### 제안(propose) = 앱 직접 푸시
- 제안은 매니저 경유가 아니라 **인플루언서 앱으로 바로 푸시**(직거래). 제안 모달의 "매니저를 통해 전달" 문구는 **제거**.
- 단 직접소통 금지는 유효 — 연락처/이메일/자유채팅은 차단(드로어 마스킹). **구조화된 캠페인 제안 푸시 = OK / 자유 연락 = 금지.**
- 중복 도배 방지(인플루언서×캠페인 상태 + 쿨다운): 미제안→전송가능 / 확인중→재전송 잠금(예 48h) / 확인함→재전송 가능. 팝업 행마다 상태 칩 + "재전송 가능 시각", 테이블엔 진행 요약(예 "제안 3·확인 1·대기 2").

### "방문 예정" 메뉴 의도
- `visit-planned.html`은 "우리 캠페인에 선정돼 방문하는 사람" 목록이 아님. **타 업체·타 캠페인 등 어떤 이유로든 한국 방문 일정이 잡힌 인플루언서 전체**를 모아 "기왕 온 김에 우리 것도" 능동 제안하는 **영업 핫리스트**. 방문비용 장벽이 해결된 사람이라 매칭 성사 쉬움(슈퍼패스 항공권 지원과 결 맞음).
- 탐색 라인: 1) 인플루언서 탐색=전체 풀 조건검색 → 2) 방문 예정=곧 올 확정자 즉시 제안(가장 적극적) → 3) 저장한 인플루언서=찜.

### 계정 관리 주체
- 클라이언트 계정은 **알파콜라보 운영팀이 발급** — self-service 없음.
- 어드민에 "계정 설정/프로필 편집/비밀번호 변경/회원 탈퇴" 페이지·메뉴 만들지 말 것. 헤더 우측도 아바타 드롭다운 X — **잔액 pill + 로그아웃만**. "계정" 영역은 빌링/정산(차감 현황·충전 내역·세금계산서)만.

### 추가 절대 금지 (개선/수리만)
- 기존 내용 **개선·수리는 OK, 새로 추가는 절대 금지** (사용자 명시).
- 금지: 새 화면/페이지, 새 메뉴, 새 필드/컬럼, 없던 데이터, 새 기능.
- 허용: 깨진 동선 복구, 죽은 버튼 살리기, 상태화면(빈/로딩/에러) 표준화, 위계·정합·일관성 정리, 토큰/비주얼 다듬기.
- `docs/GAP-AUDIT.md`의 "신설/추가" 항목은 제안만 하고 실행은 승인 후. 애매하면 "추가"로 보고 먼저 확인.

## 디자인 원칙 (필수 준수)
- **`docs/DESIGN.md`가 단일 기준** — raw hex·raw px 직접 사용 금지, Semantic/Component 토큰만 사용
- **`docs/redesign-skill.md`** 체크리스트로 AI 클리셰·일반 패턴 제거 여부 검증
- **`docs/styleseed-rules.md`** 대시보드·테이블 공통 규칙 적용
- `design-system.html`에 이미 존재하는 컴포넌트는 **그대로 재사용** (중복 생성 금지)

## 샘플 데이터 규칙 (위반 = drift)
- **국기 이모지 사용 금지** (🇯🇵 🇰🇷 🇷🇺 등) — `<span class="c-country-code">JP</span>` 칩으로만
- **테스트성 이름 금지** (`milla`, `milla2`, `test`, `test5`) — 실제같은 글로벌 인플루언서 핸들 사용
  - 예: `mika_seoul`, `natasha_mood`, `yuki_tokyo`, `kumi_osaka`, `lina_k`
- 숫자는 둥근 값(`50%`, `100,000`) 대신 유기적 값(`47.2%`, `180,000`) 사용
- Lorem Ipsum / `John Doe` / AI 클리셰(`Elevate`, `Seamless`, `Next-gen`) 금지

## 작업 규칙
1. **진단 → 개선** — 기존 화면 자료를 받으면 먼저 문제 진단, 그 다음 개선안 제작
2. **Before/After 비교 가능한 형태**로 작업
3. **컴포넌트 단위** — 사이드바·헤더는 `components/*.html`에 한 번만, 페이지별 inline 금지
4. **인수인계 고려** — CSS 변수, 컴포넌트 클래스, 네이밍 규칙 통일
5. **접근성** — ARIA, `:focus-visible`, `prefers-reduced-motion` 유지

## AI 에이전트 활용 (~/Desktop/prompt-vault/personas)

### 작업 중 수시 호출
- **`drift-watcher`** — 편집한 HTML 붙여넣기 + DESIGN.md 기준으로 일탈(국기 이모지, 테스트 이름, raw px 등) 즉시 탐지
- **`scope-watcher`** — 새 화면 착수 직전 "이 작업이 디자인 범위 내인가" 판정

### 마무리 단계
- **`design-qa-inspector`** — 화면 완성 직후 8개 카테고리 Ship/Not Ship 판정
- **`handoff-inspector`** — 커밋 직전 "개발팀 전달 가능한 형식인가" 검수

### 신규 화면 제작 시
- **`designer-ui-microcopy`** — 빈 상태/에러/로딩 문구 일괄 초안
- **`designer-mobile-ux-checklist`** — 1280/1024/768 브레이크포인트 검증

## 커밋 규율
- **작업 단위로 커밋** — 한 화면 추가, 한 카테고리 리팩터링 등
- 메시지 형식: `[scope]: 행위` 예: `list: 국기 이모지 → country-code 칩 치환`
- 대형 일괄 수정은 근거(DESIGN.md §9 등)를 본문에 기재

## 배포
- Netlify 설정: `netlify.toml`
- 프리뷰 공유는 커밋 푸시 후 Netlify가 자동 빌드
